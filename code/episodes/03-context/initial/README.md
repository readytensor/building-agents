# initial/

Pristine starting state of `md2html` for **Episode 3**.

On every run, `../agent.py` deletes `../sandbox/` and copies this directory there. The agent operates on `../sandbox/`.

**For this episode:** `md2html` as specified, possibly with one awkward naming or hook signature that's a natural refactor target (e.g., `Token` → `Node`, or an extension hook signature change that ripples across multiple files). The refactor target needs to be substantial enough that file contents legitimately fill context.

The specific refactor target will be decided as part of Episode 3 prep. See [`../../../../spec/md2html.md`](../../../../spec/md2html.md) §8 for forward-looking notes.
