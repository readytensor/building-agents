Plain text with **bold**, *italic*, and ***both***.

Underscores also work for _italic_ and __bold__.

Inline `code` with `more code`. A backslash escape: \*not italic\*.

Hard break with two trailing spaces:  
second line.

A [link with `code` inside](https://example.com/path).
